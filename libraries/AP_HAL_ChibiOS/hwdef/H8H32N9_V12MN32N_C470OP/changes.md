**Hardware:** CubePilot CubeOrange+ (STM32H757xx)
**New Powerplant:** TMotor MN8017 KV120 + TMotor V120A CAN ESC + 32" propellers
**Firmware:** ArduCopter 4.6.2

---

## 1. Hardware Reference

| Component | Specification |
| --- | --- |
| Motor | TMotor MN8017 KV120, 36N42P, 12S rated, 68.6A max, ~16.8 kg thrust/motor |
| ESC | TMotor V120A, 14S max, 60A continuous, DroneCAN/FOC, throttle 1040–1940 µs |
| Propeller | 32" |
| Frame | OctaQuad X (8 motors) |
| Flight Controller | CubeOrange+ (STM32H757xx) |

> ⚠️ The V120A ESC is rated at **60A continuous**. The motor can draw up to 68.6A at 100% throttle.
Sustained full-throttle operation risks ESC overheating. Monitor ESC temperature during initial flights.
> 

---

## 2. Parameter Changes

### 2.1 Pole Count — No Change

```
ESC_HW_POLES    42   (unchanged)
SERVO_BLG_POLES 42   (unchanged)
SERVO_FTW_POLES 42   (unchanged)
```

The MN8017 is 36N42P = **42 magnetic poles**. The previous motor also had 42 poles so no change was needed. ArduPilot uses this value to calculate RPM from ESC electrical frequency.

---

### 2.2 CAN ESC Routing — Added

```
CAN_D1_UC_ESC_BM  255   (added — was not set)
```

**Why:** `255` = `0b11111111` = all 8 motor channels routed through DroneCAN bus 1. Required for the V120A ESC operating in DroneCAN mode.

> Reference: [ArduPilot DroneCAN ESCs](https://ardupilot.org/copter/docs/common-uavcan-escs.html)
> 

---

### 2.3 PWM Range — Updated

```
MOT_PWM_MIN     1040   (was 1100)
SERVO1_MIN      1040   (was 1100)
...
SERVO8_MIN      1040   (was 1100)

SERVO1_TRIM ... SERVO8_TRIM  1490   (was 1500)
```

The V120A ESC throttle range starts at **1040 µs**, not 1100 µs. Using 1100 as minimum discards the bottom 60 µs of the ESC range, compressing the effective control band and shifting MOT_SPIN_ARM/MIN calibration. The trim was lowered to 1490 to centre correctly within the 1040–1940 range.

> Reference: TMotor V120A datasheet — throttle range 1040–1940 µs
> 

---

### 2.4 Motor Thrust Expo — Updated

![image.png](attachment:837378d5-4ae5-41b0-a1de-4ab3d1bd370a:image.png)

```
MOT_THST_EXPO   0.78   (was 0.5613)
```

The value **0.78** was generated by **Mission Planner Initial Parameters** tool with 32" prop and OctaQuad X frame as input. This is the recommended method per the Methodic Configurator guide (step `15_motor.param`).

The expo value linearises the thrust curve: `T(a) = expo × a + (1 − expo) × a²`. A higher value (0.78) reflects the more non-linear thrust curve of large-diameter low-pitch props.

---

### 2.5 Hover Throttle — Reset

```
MOT_THST_HOVER  0.2   (was 0.2570658)
```

The value **0.2** is the Mission Planner estimate for this motor/prop combination. ArduPilot **automatically learns** the real value during the first stable hover in ALTHOLD mode (`MOT_HOVER_LEARN = 2` = learn and save). After ~2 minutes of stable hover the parameter converges and saves automatically.

After the value is learned, **update `INS_HNTCH_REF` to match** the new `MOT_THST_HOVER`.

> Reference: [ArduPilot Hover Throttle](https://ardupilot.org/copter/docs/ac_throttle.html)
Reference: Methodic Configurator — step `20_throttle_controller.param`
> 

---

### 2.6 Motor Spin Values — Updated

```
MOT_SPIN_ARM  0.12   (was 0.06)
MOT_SPIN_MIN  0.15   (was 0.09)
```

CAN FOC ESCs (like the V120A) require more command headroom before they begin spinning reliably compared to standard PWM ESCs. These values **must be verified on the bench** before first flight:

1. **MOT_SPIN_ARM**: Props OFF — use Mission Planner Motor Test, raise throttle % from 0 until all motors spin reliably. That % (as a fraction) = MOT_SPIN_ARM.
2. **MOT_SPIN_MIN**: Props ON (on a scale) — raise until consistent, stable thrust is produced. That % = MOT_SPIN_MIN.
3. Rule: `MOT_SPIN_MIN ≥ MOT_SPIN_ARM + 0.03` (currently 0.15 − 0.12 = 0.03 ✓)

---

### 2.7 Attitude Controller — Reset for Fresh Tuning

```
ATC_ACCEL_P_MAX  19100   (was 30000)   ← Mission Planner Initial Parameters
ATC_ACCEL_R_MAX  19100   (was 30000)   ← Mission Planner Initial Parameters
ATC_ACCEL_Y_MAX   8000   (was 7000)    ← Mission Planner Initial Parameters

ATC_ANG_PIT_P     4.5    (was 8)       ← conservative reset
ATC_ANG_RLL_P     4.5    (was 8)       ← conservative reset
ATC_ANG_YAW_P     4.5    (was 5.4)     ← conservative reset

ATC_RAT_PIT_P     0.08   (was 0.12)    ← conservative reset
ATC_RAT_PIT_I     0.08   (was 0.12)    ← conservative reset
ATC_RAT_PIT_D     0.002  (was 0.005)   ← conservative reset

ATC_RAT_RLL_P     0.08   (was 0.12)    ← conservative reset
ATC_RAT_RLL_I     0.08   (was 0.12)    ← conservative reset
ATC_RAT_RLL_D     0.002  (was 0.005)   ← conservative reset

ATC_RAT_YAW_P     0.5    (was 0.6401945)
ATC_RAT_YAW_I     0.05   (was 0.06401945)
ATC_RAT_YAW_FLTE  2      (was 1)       ← Mission Planner Initial Parameters
ATC_RAT_YAW_FLTT  10     (was 15)

ATC_INPUT_TC      0.2    (was 0.15)    ← slightly smoother for large frame
ATC_SLEW_YAW     3000    (was 6000)    ← slower slew appropriate for large heavy frame
```

- `ATC_ACCEL_P/R_MAX` and `ATC_ACCEL_Y_MAX` are from **Mission Planner Initial Parameters** for 32" prop OctaQuad X. These values define the maximum angular acceleration the controller will command and are frame/mass specific.
- **All rate P/I/D and angle P gains were reset to conservative safe starting values.** The previous values were from Autotune on the old powerplant — they cannot be reused on a completely different motor/prop combination.
- Autotune must be run on the new powerplant to find the final values.

> Reference: [ArduPilot Tuning Process Instructions](https://ardupilot.org/copter/docs/tuning-process-instructions.html)
Reference: Methodic Configurator — Section 6.11 (`11_initial_atc.param`)
> 

---

### 2.8 Harmonic Notch Filter — Conservative Initial Estimate

```
INS_HNTCH_FREQ   31    (was 34)          ← estimated from 32" prop hover RPM
INS_HNTCH_REF    0.2   (was 0.1986204)   ← must equal MOT_THST_HOVER
INS_HNTCH_ATT   40    (was 80)           ← conservative start
INS_HNTCH_BW    15    (was 20)           ← conservative start
INS_HNTCH_HMNCS  3    (was 15)           ← 1st + 2nd harmonics only
INS_HNTCH_MODE   1    (unchanged — throttle-based)
INS_HNTCH_OPTS  16    (unchanged — double-notch)
INS_GYRO_FILTER 20    (unchanged)
INS_ACCEL_FILTER 10   (unchanged)
```

**`INS_HNTCH_FREQ 31 Hz`** — Estimated hover RPM for the MN8017 with 32" props:

- Hover throttle ≈ 20% of max thrust → ~3370 g/motor
- From TMotor thrust table interpolation at ~3370 g: ≈ 1875 RPM
- Notch frequency = 1875 RPM ÷ 60 = **31 Hz**
- (Previous 34 Hz was for 30" props; 32" props spin slower for the same thrust)

**`INS_HNTCH_REF 0.2`** — Must equal `MOT_THST_HOVER`. The throttle-based notch tracks frequency as:

```
f_notch = FREQ × sqrt(throttle / REF)
```

At hover (throttle = REF), the notch sits exactly at FREQ. After the first hover updates `MOT_THST_HOVER`, update `INS_HNTCH_REF` to the same learned value.

**`INS_HNTCH_ATT 40 dB`** — Reduced from 80 dB. Starting conservatively; aggressive attenuation before confirming the correct frequency adds unwanted phase lag.

**`INS_HNTCH_BW 15 Hz`** — Narrower bandwidth to start. Widen only after log review confirms the noise peak is broad.

**`INS_HNTCH_HMNCS 3`** — Bitmask `0b0011` = harmonics 1 and 2 only. Large 32" props generate most noise at the fundamental and 2nd harmonic; higher harmonics are minimal.

> ⚠️ These are **pre-flight estimates** per Methodic Configurator step `18_notch_filter_setup.param`. They must be refined using the Filter Review Tool after the first flight log.
> 

> Reference: [ArduPilot Harmonic Notch Filter](https://ardupilot.org/copter/docs/common-imu-notch-filtering.html)
Reference: Methodic Configurator — Section 6.16
> 

---

### 2.9 Battery Thresholds — Updated

```
BATT_LOW_VOLT   43.2   (was 43.7)
BATT_CRT_VOLT   42.0   (was 41.5)

BATT2_LOW_VOLT  43.2   (was 43.7)
BATT2_CRT_VOLT  42.0   (was 41.5)

BATT_ARM_VOLT   46.0   (unchanged — operational choice)
BATT2_ARM_VOLT  46.0   (unchanged — operational choice)
```

Low and critical voltage thresholds were updated to the values recommended by **Mission Planner Initial Parameters** for a 12S configuration:

- **43.2 V** = 3.6 V/cell × 12 cells (low, triggers RTL)
- **42.0 V** = 3.5 V/cell × 12 cells (critical, triggers Land)

The arm voltage (46.0 V) was kept as an operational choice — it serves as a minimum state-of-charge gate before arming and was not changed from the source model.

---

### 2.10 Battery Low Threshold — Revised

```
BATT_LOW_VOLT   43.5   (was 43.2)
BATT2_LOW_VOLT  43.5   (was 43.2)
```

Low voltage threshold raised from 43.2 V (3.6 V/cell) to **43.5 V** (3.625 V/cell × 12 cells). This provides an earlier RTL warning to account for faster voltage sag under load with aged cells or in cold conditions.

---

### 2.11 Flight Mode Channel — Disabled

```
FLTMODE_CH   0   (added — was not set)
```

Setting `FLTMODE_CH = 0` disables RC flight mode switching via a dedicated transmitter channel. Flight mode changes are handled exclusively via GCS or companion computer. This prevents accidental mode changes from RC stick input during autonomous operations.

---

### 2.12 GPS GNSS Constellation and SBAS — Updated

```
GPS_SBAS_MODE    0   (added — SBAS disabled)
GPS1_GNSS_MODE   5   (added — GPS + Galileo)
GPS2_GNSS_MODE   5   (added — GPS + Galileo)
```

- **`GPS_SBAS_MODE 0`** — Disables SBAS corrections. SBAS coverage and reliability varies by region and can cause GPS instability when the augmentation satellite geometry is poor.
- **`GPS1_GNSS_MODE 5` / `GPS2_GNSS_MODE 5`** — Bitmask `0b00000101` = GPS (bit 0) + Galileo (bit 2). Restricts both receivers to GPS and Galileo constellations. Galileo provides better geometry and accuracy than GLONASS in most operating regions and produces a cleaner, more stable CAN bus packet rate.